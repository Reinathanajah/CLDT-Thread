{
  // This authoring copy creates a paired scheduler experiment, not a generic claim that SMP is faster.
  "schema_version": "2.0",
  "state": "template",
  "experiment_id": "smp-comparison",
  "title": "ESP32-S3 SMP Comparison",
  "purpose": {
    "question": "How does the same gateway workload behave under ESP-IDF SMP and CONFIG_FREERTOS_UNICORE?",
    "comparison": "Paired physical runs that differ only in the gateway scheduler build and its recorded binary hash.",
    "primary_metric": "Critical on-time delivery alongside gateway queue delay and per-core task evidence."
  },
  "setup": {
    // Record identical endpoint/RCP builds and two gateway build identities. Only the declared scheduler setting may differ.
    "nodes": null,
    "thread_channel": null,
    "placement": null,
    "firmware_reference": null
  },
  "execution": {
    "warmup_s": null,
    "measurement_s": null,
    "cooldown_s": null,
    // Define matched pair count and a pair-level exclusion rule before measuring.
    "repetitions": null,
    // Use the same deterministic workload seed in every matched pair.
    "seed": null
  },
  "traffic": {
    // Copy the completed stable baseline streams exactly.
    "streams": null
  },
  "scenario": {
    // Usually event none. Target should name the scheduler comparison or no injected disturbance.
    "event": null,
    "at_s": null,
    "duration_s": null,
    "target": null
  },
  "treatment": {
    // Use smp mode with no remote action unless the primary safety chain is already proven separately.
    "mode": null,
    "candidate_action": null,
    "control_profile": null,
    "host_model": null,
    "remote_actuation": null
  },
  "acceptance": {
    "counter_reconciliation": null,
    "minimum_critical_on_time_pdr": null,
    // State pair invalidation: changed topology, missing core trace, or nonmatching manifest/binary identity.
    "negative_case": null
  },
  "evidence": {
    // Archive both sdkconfig files, compiler flags, binary hashes, task/core trace, queue delays, and final counters.
    "required_artifacts": null,
    "operator_notes_required": null,
    "topology_photo_required": null
  },
  "_todo": [
    {
      "path": "/setup",
      "action": "Build a truly matched SMP/unicore gateway pair.",
      "method": "Use one source commit; retain both build configurations and prove endpoint/RCP firmware is unchanged.",
      "done_when": "Only the declared scheduler configuration differs between the gateway binaries."
    },
    {
      "path": "/execution",
      "action": "Run pairs within one stable physical block.",
      "method": "Alternate or randomize condition order, reboot the gateway between conditions, and retain only complete matched pairs.",
      "done_when": "The file states repetitions, seed plan, and pair exclusion rule."
    },
    {
      "path": "/evidence",
      "action": "Capture scheduler evidence, not only network counters.",
      "method": "Archive raw task/core trace, queue delays, stack margin, topology, versions, and counters.",
      "done_when": "Radio variation can be separated from a plausible scheduler effect."
    }
  ]
}
